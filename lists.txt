val palavra = "Niagara"

val toCharList = fn x => String.explode x

val toStringList = fn x => Char.toString x

val frags = ["Ni", "a", "g", "r"]

val verificar = toChar palavra

fun parse(_,[]) = false
  | parse(texto,h::t) =
  	if texto = h
  		then true
  		else parse(texto,t)
  		
val tokens = List.map toString verificar
   	     	 
val tok = fn (t,n) => List.nth(t,n)

val a = tok (tokens,0)^tok (tokens,1)


* 1 - Transformar palavra em lista de char -> val toCharList *
* 2 - Converter toCharList em lista de String -> val toStringList *
* 3 - Tentar parse(toStringList[0],frags) 
					  T-> parser(toStringList[1],frags)
					  F-> parse(toStringList[0]^toStringList[1],frags) *

* 4 - Iterar passo 3 até parse(toStringList[n] ou passo 3 == false
