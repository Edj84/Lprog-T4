Alice 1.4 ("Kraftwerk 'Equaliser' Album") mastered 2007/04/24
### loaded signature from x-alice:/lib/system/Print
### loaded signature from x-alice:/lib/tools/Inspector
### loaded signature from x-alice:/lib/distribution/Remote
> fun readText(filename) =
    let val file = TextIO.openIn filename
        val text = TextIO.inputAll file
        val _ = TextIO.closeIn file
    in String.tokens (fn c => c = #"\n") text
    end;
val readText : string -> string list = _fn
> val test_file = readText "'yourPath'/file.txt";
Uncaught exception
   Io {cause =
          SysErr
             ("O sistema não pode encontrar o caminho especificado. ", SOME 3),
       function = "openIn", name = "'yourPath'/file.txt"}
> fun writeText(filename) =
    let val file = TextIO.openOut(filename)
        val _ = TextIO.output(file, "This is an example\n")
    in TextIO.closeOut(file)
    end;
    val _ = writeText "'yourPath'/file.txt";
    